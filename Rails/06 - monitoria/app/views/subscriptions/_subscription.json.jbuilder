json.extract! subscription, :id, :nota, :cre, :selecionado, :form_voluntario, :form_relatorio, :discipline_id, :semester_id, :student_id, :created_at, :updated_at
json.url subscription_url(subscription, format: :json)
